import type { Multer, Options } from "multer";
export interface MULTER_MODULE {
    multer: Multer;
    get(options: Options): Multer;
}
export declare const MULTER_MODULE: unique symbol;
export declare const PLATFORM_MULTER_OPTIONS: unique symbol;
