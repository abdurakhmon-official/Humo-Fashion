import type { TokenProvider } from "../interfaces/TokenProvider.js";
/**
 * Lazy load a provider from his package and invoke only when the provider is used.
 */
export declare function lazyInject<Token>(factory: () => Promise<{
    default: TokenProvider<Token>;
}>): Promise<Token>;
export declare function optionalLazyInject<Token>(factory: () => Promise<{
    default: TokenProvider;
}>): Promise<Token | undefined>;
