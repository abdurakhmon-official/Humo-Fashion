import { type PlatformMulterFile } from "../../common/index.js";
declare const kMultipart: unique symbol;
declare module "fastify" {
    interface FastifyRequest {
        files?: {
            [key: string]: PlatformMulterFile | PlatformMulterFile[];
        };
        [kMultipart]: boolean;
    }
}
export {};
